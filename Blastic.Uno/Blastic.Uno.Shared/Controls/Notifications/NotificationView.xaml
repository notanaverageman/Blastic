<UserControl x:Class="Blastic.Controls.Notifications.NotificationView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:controlExtensions="using:Blastic.ControlExtensions"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:dynamicControls="using:Blastic.Uno.Shared.Controls.DynamicControls"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:notifications="using:Blastic.Services.Notifications"
			 mc:Ignorable="d"
			 d:DataContext="{d:DesignInstance notifications:Notification}">

	<Border BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
			BorderThickness="2">
		<Grid Padding="8"
			  controlExtensions:GridExtensions.RowDefinitions="Auto,*"
			  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
			  CornerRadius="0">

			<Button Grid.Row="0"
					HorizontalAlignment="Right"
					Command="{Binding Dismiss}"
					Style="{StaticResource LightweightButton}">
				<SymbolIcon Symbol="Cancel" />
			</Button>

			<dynamicControls:DynamicControl Grid.Row="1"
											Margin="8"
											Model="{x:Bind Notification.Model, Mode=OneWay}" />
		</Grid>
	</Border>
</UserControl>