<UserControl x:Class="Blastic.Wpf.Sample.UserInterface.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:execution="clr-namespace:Blastic.Wpf.Execution;assembly=Blastic.Wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:userInterface="clr-namespace:Blastic.Wpf.Sample.UserInterface"
             xmlns:controlExtensions="clr-namespace:Blastic.Wpf.ControlExtensions;assembly=Blastic.Wpf"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance userInterface:HomeViewModel}">
	<Grid controlExtensions:GridExtensions.RowDefinitions="Auto,*">
		<StackPanel Grid.Row="0"
		            Orientation="Horizontal"
		            HorizontalAlignment="Center"
		            VerticalAlignment="Center">

			<Button Command="{Binding AddCustomerCommand}"
			        Content="Add Customer" />

			<Button Command="{Binding RefreshCustomersCommand}"
			        Content="Refresh Customers" />
		</StackPanel>

		<DataGrid
			Grid.Row="1"
			IsReadOnly="True"
			ItemsSource="{Binding Customers}" />

		<execution:ExecutionContextView
			Grid.Row="0"
			Grid.RowSpan="2"
			ExecutionContext="{Binding ExecutionContext}" />
	</Grid>
</UserControl>