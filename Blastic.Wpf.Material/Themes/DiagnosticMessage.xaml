<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:converters="clr-namespace:Blastic.Wpf.Converters;assembly=Blastic.Wpf"
					xmlns:diagnostics="clr-namespace:Blastic.Diagnostics;assembly=Blastic"
					xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="pack://application:,,,/Blastic.Wpf;component/Themes/Colors.xaml" />
		<ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
	</ResourceDictionary.MergedDictionaries>

	<Style TargetType="{x:Type Rectangle}" />

	<DataTemplate DataType="{x:Type diagnostics:DiagnosticMessage}">
		<Grid HorizontalAlignment="Left"
			  ToolTip="{Binding Message.Value}">

			<Grid.Resources>
				<converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
			</Grid.Resources>

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"
								  SharedSizeGroup="Icons" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<materialDesign:PackIcon Grid.Column="0"
									 Height="{Binding ActualWidth, RelativeSource={RelativeSource Self}}"
									 Margin="0,0,8,0"
									 VerticalAlignment="Center">

				<materialDesign:PackIcon.Style>
					<Style TargetType="materialDesign:PackIcon">
						<Style.Triggers>
							<DataTrigger Binding="{Binding Severity.Value}"
										 Value="Error">
								<Setter Property="Kind" Value="AlertOctagon" />
								<Setter Property="Foreground" Value="{StaticResource BlasticRedBrushMedium}" />
							</DataTrigger>
							<DataTrigger Binding="{Binding Severity.Value}"
										 Value="Warning">
								<Setter Property="Kind" Value="Alert" />
								<Setter Property="Foreground" Value="{StaticResource BlasticOrangeBrushMedium}" />
							</DataTrigger>
							<DataTrigger Binding="{Binding Severity.Value}"
										 Value="Info">
								<Setter Property="Kind" Value="AlertCircle" />
								<Setter Property="Foreground" Value="{StaticResource BlasticBlueBrushMedium}" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</materialDesign:PackIcon.Style>
			</materialDesign:PackIcon>

			<TextBlock Grid.Column="1"
					   VerticalAlignment="Center"
					   FontSize="12"
					   Text="{Binding Message.Value}"
					   TextTrimming="WordEllipsis" />

			<Button Grid.Column="2"
					Height="20"
					Margin="16,0,0,0"
					VerticalAlignment="Center"
					materialDesign:RippleAssist.IsDisabled="True"
					Command="{Binding ActionCommand}"
					Content="{Binding ActionLabel.Value}"
					FontSize="12"
					Style="{StaticResource MaterialDesignToolButton}"
					ToolTip="{Binding Content, RelativeSource={RelativeSource Self}}"
					Visibility="{Binding ActionCommand, Converter={StaticResource NullToVisibilityConverter}}" />
		</Grid>
	</DataTemplate>
</ResourceDictionary>