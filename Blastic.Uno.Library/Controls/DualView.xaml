<UserControl x:Class="Blastic.Controls.DualView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:converters="using:Blastic.Converters"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d">

	<UserControl.Resources>
		<converters:InverseVisibilityConverter x:Key="InverseVisibilityConverter" />
		<converters:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
	</UserControl.Resources>

	<Grid>
		<ContentPresenter Name="First"
						  Content="{x:Bind FirstView, Mode=OneWay}"
						  Visibility="{x:Bind ShowFirstView, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

		<ContentPresenter Content="{x:Bind SecondView, Mode=OneWay}"
						  Visibility="{Binding Visibility, ElementName=First, Converter={StaticResource InverseVisibilityConverter}}" />
	</Grid>
</UserControl>
