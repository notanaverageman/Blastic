<Page x:Class="Blastic.UserInterface.TabbedMain.TabbedMainView"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:controls="using:Blastic.Controls"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:dynamicControls="using:Blastic.Controls.DynamicControls"
	  xmlns:main="using:Blastic.UserInterface.TabbedMain"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:notifications="using:Blastic.Controls.Notifications"
	  mc:Ignorable="d"
	  d:DataContext="{d:DesignInstance main:TabbedMainViewModel}">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<!--<controls:TabView ItemsSource="{Binding Items}"
		                  SelectedItem="{Binding ActiveItem.Value}">
			<controls:TabView.ItemHeaderTemplate>
				<DataTemplate x:DataType="main:IMainTab">
					<TextBlock Text="{Binding Title.Value}" />
				</DataTemplate>
			</controls:TabView.ItemHeaderTemplate>
			<controls:TabView.ItemTemplate>
				<DataTemplate x:DataType="main:IMainTab">
					<ContentControl HorizontalContentAlignment="Stretch"
					                VerticalContentAlignment="Stretch"
					                viewManagement:View.Model="{Binding}"
					                IsTabStop="False" />
				</DataTemplate>
			</controls:TabView.ItemTemplate>
		</controls:TabView>-->

		<Button Command="{Binding OpenCommand}"
				Content="Test" />

		<dynamicControls:Form ExecutionContext="{Binding ExecutionContext}" />

		<controls:BusyIndicator ExecutionContext="{Binding ExecutionContext}" />

		<notifications:NotificationAreaView HorizontalAlignment="Right"
											VerticalAlignment="Bottom"
											NotificationService="{Binding ExecutionContext.NotificationService}" />
	</Grid>
</Page>