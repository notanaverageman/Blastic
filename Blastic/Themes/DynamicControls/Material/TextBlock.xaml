<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:controlExtensions="clr-namespace:Blastic.ControlExtensions"
					xmlns:converters="clr-namespace:Blastic.Converters"
					xmlns:help="clr-namespace:Blastic.Controls.Help"
					xmlns:label="clr-namespace:Blastic.Controls.DynamicControls.Elements.Label"
					xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="/Blastic;component/Themes/DynamicControls/Material/Common.xaml" />
		<ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/materialdesigntheme.TextBox.xaml" />
	</ResourceDictionary.MergedDictionaries>

	<converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

	<Style TargetType="{x:Type label:LabelPresenter}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type label:LabelPresenter}">
					<Grid controlExtensions:GridExtensions.ColumnDefinitions="Auto,*,Auto">
						<materialDesign:PackIcon Grid.Column="0"
												 Width="{Binding IconSize, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}}"
												 Height="{Binding IconSize, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}}"
												 Margin="{Binding IconMargin, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}}"
												 Kind="{Binding IconKind.Value, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}}"
												 Style="{StaticResource MaterialColoredIconStyle}"
												 Visibility="{Binding IconKind.Value, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}, Converter={StaticResource NullToVisibilityConverter}}" />

						<TextBlock Name="PropertyControl"
								   Grid.Column="1"
								   Margin="0,-14,0,0"
								   VerticalAlignment="Center"
								   Text="{Binding Property.Value, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}, UpdateSourceTrigger=PropertyChanged}" />

						<help:HelpView Grid.Column="2"
									   HelpContent="{Binding Help.Value, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}}"
									   Visibility="{Binding Help.Value, RelativeSource={RelativeSource AncestorType=label:LabelPresenter}, Converter={StaticResource NullToVisibilityConverter}}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>