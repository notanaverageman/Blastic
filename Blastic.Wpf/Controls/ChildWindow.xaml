<UserControl x:Class="Blastic.Controls.ChildWindow"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:controlExtensions="clr-namespace:Blastic.ControlExtensions"
			 xmlns:controls="clr-namespace:Blastic.Controls"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d">

	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
	</UserControl.Resources>

	<Border HorizontalAlignment="Stretch"
	        VerticalAlignment="Stretch"
	        BorderBrush="Black"
	        Background="#88DDDDDD"
	        BorderThickness="2"
	        Visibility="{Binding IsOpen, RelativeSource={RelativeSource AncestorType=controls:ChildWindow}, Converter={StaticResource BooleanToVisibilityConverter}}">

		<Grid HorizontalAlignment="Center"
		      VerticalAlignment="Center"
		      controlExtensions:GridExtensions.RowDefinitions="*,Auto">

			<ScrollViewer HorizontalScrollBarVisibility="Disabled"
			              IsTabStop="False"
			              VerticalScrollBarVisibility="Disabled">

				<Grid>
					<ContentPresenter Content="{Binding ChildContent, RelativeSource={RelativeSource AncestorType=controls:ChildWindow}}" />
				</Grid>
			</ScrollViewer>
		</Grid>
	</Border>
</UserControl>