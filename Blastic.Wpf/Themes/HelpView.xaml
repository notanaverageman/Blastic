<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:controlExtensions="clr-namespace:Blastic.Wpf.ControlExtensions"
					xmlns:controls="clr-namespace:Blastic.Wpf.Controls">

	<Style TargetType="{x:Type controls:HelpView}">
		<Setter Property="VerticalContentAlignment" Value="Center" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:HelpView}">
					<Grid Margin="{TemplateBinding Padding}"
						  controlExtensions:GridExtensions.ColumnDefinitions="*,Auto">

						<FrameworkElement Name="DataContextProxy"
										  Tag="{Binding HelpContent, RelativeSource={RelativeSource AncestorType=controls:HelpView}}" />

						<ContentPresenter Grid.Column="0"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  Content="{TemplateBinding Content}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>