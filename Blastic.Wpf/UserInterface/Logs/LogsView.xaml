<Window x:Class="Blastic.Wpf.UserInterface.Logs.LogsView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:controlExtensions="clr-namespace:Blastic.ControlExtensions"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:logging="clr-namespace:Microsoft.Extensions.Logging;assembly=Microsoft.Extensions.Logging.Abstractions"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:controlExtensions1="clr-namespace:Blastic.Wpf.ControlExtensions"
		Title="{Binding Title.Value}"
		mc:Ignorable="d"
		Width="1400"
		Height="800"
		d:DataContext="{d:DesignInstance logs:LogsViewModel}"
		Style="{StaticResource WindowStyle}">

	<Grid controlExtensions1:GridExtensions.RowDefinitions="Auto,*">
		<Grid Grid.Row="0">

			<ComboBox Margin="16,0,0,0"
					  HorizontalAlignment="Left"
					  IsReadOnly="True"
					  ItemsSource="{Binding LogLevels}"
					  SelectedItem="{Binding MinimumLogLevel.Value, Mode=TwoWay}" />

			<Button Margin="0,0,8,0"
					HorizontalAlignment="Right"
					Command="{Binding Clear}"
					Content="Clear" />
		</Grid>

		<DataGrid Grid.Row="1"
				  AutoGenerateColumns="False"
				  IsReadOnly="True"
				  ItemsSource="{Binding Logs}">

			<DataGrid.Columns>
				<DataGridTextColumn Width="Auto"
									Binding="{Binding Date}"
									Header="Date" />

				<DataGridTextColumn Width="Auto"
									Binding="{Binding Level}"
									Header="Level" />

				<DataGridTextColumn Width="*"
									Binding="{Binding Message}"
									Header="Message">

					<DataGridTextColumn.ElementStyle>
						<Style>
							<Setter Property="TextBlock.TextWrapping" Value="Wrap" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
			</DataGrid.Columns>

			<DataGrid.RowStyle>
				<Style BasedOn="{StaticResource {x:Type DataGridRow}}"
					   TargetType="DataGridRow">

					<Style.Triggers>
						<DataTrigger Binding="{Binding Level}"
									 Value="{x:Static logging:LogLevel.Critical}">

							<Setter Property="Foreground" Value="DarkRed" />
						</DataTrigger>

						<DataTrigger Binding="{Binding Level}"
									 Value="{x:Static logging:LogLevel.Error}">

							<Setter Property="Foreground" Value="Red" />
						</DataTrigger>

						<DataTrigger Binding="{Binding Level}"
									 Value="{x:Static logging:LogLevel.Warning}">

							<Setter Property="Foreground" Value="DarkOrange" />
						</DataTrigger>

						<DataTrigger Binding="{Binding Level}"
									 Value="{x:Static logging:LogLevel.Information}">

							<Setter Property="Foreground" Value="CornflowerBlue" />
						</DataTrigger>

						<DataTrigger Binding="{Binding Level}"
									 Value="{x:Static logging:LogLevel.Debug}">

							<Setter Property="Foreground" Value="Green" />
						</DataTrigger>

						<DataTrigger Binding="{Binding Level}"
									 Value="{x:Static logging:LogLevel.Trace}">

							<Setter Property="Foreground" Value="DarkGray" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</DataGrid.RowStyle>
		</DataGrid>
	</Grid>
</Window>